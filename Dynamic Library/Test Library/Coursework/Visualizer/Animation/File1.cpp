//---------------------------------------------------------------------------

#include <vcl.h>
#pragma hdrstop

#include <tchar.h>
#include "TbAbstractAction.h"
#pragma argsused
#include "TbAbstractAction.h"
#include <map.h>
#include "scene_func.h"
//---------------------------------------------------------------------------
// назва автомата - номер (id)
// назва стана - номер (id)
// назва ребра - номер (id)
/*class TbLibrary
{
	private:
		map<String, int> stateName; // по назві визначається id.
		map<int, State*> states;

		map<State*, State*> states; // ключ як вказівник
};
//---------------------------------------------------------------------------
class TbScene
{
	private:
		map<int, State*> states; // int - name id
	public:
		bool addLink(int from, int to, String symbol)
		{
			State *stateFrom = states.find(from);
			State *stateTo = states.find(to);
			if(stateFrom && stateTo)
			{

            }
        }
};
//---------------------------------------------------------------------------
class TbGroupState
{
	private:
		set<int> states;
};
//---------------------------------------------------------------------------
class TbLink
{
	private:
		String symbols;
};
//---------------------------------------------------------------------------
class TbState
{
	private:
		map<TbState*, TbLink*> links; // TbState* - to
	public:
		bool addLink()
};
//---------------------------------------------------------------------------
class TbState
{
	private:
		set<int> names;
		int firstState;
		set<int> finaleStates;
};*/
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
//---------------------------------------------------------------------------
int _tmain(int argc, _TCHAR* argv[])
{
	return 0;
}
//---------------------------------------------------------------------------
